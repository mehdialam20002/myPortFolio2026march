[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# Friendly endpoint -> serverless function (keeps the API key server-side)
[[redirects]]
  from = "/api/ask"
  to = "/.netlify/functions/ask"
  status = 200
